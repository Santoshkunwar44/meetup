import {styled} from "styled-components"
export const UserSkeletonWrapper = styled.div`
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    .skeleton{
        border-radius: 6px;
    }
`