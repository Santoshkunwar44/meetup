import {styled} from "styled-components"

export const StartChatWrapper  = styled.div`
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin: auto;
    align-items: center;

    img{
        width: 200px;
    }
    .sayHiText{
        font-size: 1rem;
        color: var(--fade_text);
        font-weight: 800;
        letter-spacing: 1px;
        text-align: center;
    }

`